{
  "name": "create-react-features-folder",
  "displayName": "create-react-features-folder",
  "description": "create folder structure with all its sub folder for feature",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.88.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "create-react-features-folder.createReactFeature"
  ],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "create-react-features-folder.createReactFeature",
        "title": "Create React Project New Feature"
      }
    ]
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "npm run lint",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.88.0",
    "@types/mocha": "^10.0.6",
    "@types/node": "18.x",
    "eslint": "^8.57.0",
    "typescript": "^5.3.3",
    "@vscode/test-cli": "^0.0.8",
    "@vscode/test-electron": "^2.3.9"
  }
}
